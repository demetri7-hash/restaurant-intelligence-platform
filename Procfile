# Procfile for Koyeb Deployment
# This tells Koyeb what command to run to start the application

# Start the backend API server (main application)
web: cd backend && npm start

# Alternative: Start both frontend and backend (if needed)
# web: npm start